function printCityInfo(firstName, lastName, age) {
    const person = {
        firstName: firstName,
        lastName: lastName,
        age: Number(age)
    };

    return person;
}

printCityInfo('Peter', 'Pan', '20');
